language: node_js

node_js:
  - 8

install:
  - npm install

script:
  - npm run e2e 
  - npm run layout

addons:
  artifacts: true
    target_paths:
    - /${TRAVIS_REPO_SLUG}/${TRAVIS_BUILD_NUMBER}/${TRAVIS_JOB_NUMBER}