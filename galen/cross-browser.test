@@ set 
	baseUrl https://wordpress.com/log-in
    run_in_grid selenium grid http://your.selenium.grid.hostname:4444/wd/hub 

@@ parameterized
    | browser  |
    | firefox  |
    | chrome   |

@@ parameterized
    | deviceName | tags      | size     |
    | Mobile     | mobile    | 320x600  |
    | Tablet     | tablet    | 640x480  |
    | Desktop    | desktop   | 1024x800 |

@@ groups mobile, login, desktop, tablet  
Login page on ${deviceName} device in ${browser} browser
    ${run_in_grid} ${browser} ${baseUrl} ${size}
        check specs/loginpage.gspec --include "${tags}"
